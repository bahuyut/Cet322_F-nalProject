@model IEnumerable<EduHub.Models.Grade>

@{
    ViewData["Title"] = "My Grades";
}

<h2>My Grades</h2>

<table class="table">
    <thead>
        <tr>
            <th>
                Assignment Title
            </th>
            <th>
                Score
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.Assignment?.Title
                </td>
                <td>
                    @item.Score
                </td>
            </tr>
        }
    </tbody>
</table>
